Program TESTE123;
    Var nome : Integer;
    Var X, Y, Z : Integer;
    (*Comentario*)
Begin
    nome := 'Filipi Souza';
    (*Comentario novo*)
    Readln(x); 
    X := 1000;
    Z := (X * Y);
    Writeln ('O RESULTADO ESTA OK');
    (*Mais um comentario*)
End;